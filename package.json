{
  "name": "@uverify/ui-template",
  "version": "1.2.1",
  "description": "A tool suite for creating custom UVerify certificate UIs.",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "clean": "rm -rf node_modules && cd packages/blueprint && npm run clean && cd ../core && npm run clean && cd ../cli && npm run clean"
  }
}
